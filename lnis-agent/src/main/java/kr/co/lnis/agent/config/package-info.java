/**
 * Windows Agent의 ID, 역할, 중앙 서버 주소, 인증 token과 네이티브 DLL 경로를 읽는다.
 *
 * <p>환경 변수, JVM 시스템 속성, properties 파일, 기본값 순서로 설정을 결정해 콘솔 실행과 Windows
 * 서비스 실행에서 같은 설정 규칙을 사용한다.
 */
package kr.co.lnis.agent.config;
